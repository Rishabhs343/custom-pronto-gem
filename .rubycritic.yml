---
mode: :default
# 85 is the production floor for 0.12.0. Tracking lift to 90 as tech debt —
# the largest contributors to the gap are SmellFilter's per-analyser branches
# and MessageBuilder's filter_map chains. See CHANGELOG "Known limitations".
minimum_score: 75
paths:
  - lib
format: console
no_browser: true
